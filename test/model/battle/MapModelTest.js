import assert from 'power-assert';
import MapModel from '../../../src/model/battle/MapModel.js';

describe('MapModelクラス', () => {
  beforeEach(done => {
    done();
  });
  describe('isMovableメソッド', () => {
    it('移動可能なマップチップでかつ障害物がない場合', () => {
    });
  });
});